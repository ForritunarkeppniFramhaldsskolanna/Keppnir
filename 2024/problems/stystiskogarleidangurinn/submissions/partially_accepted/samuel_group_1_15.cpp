#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, m, s;
    cin >> n >> m >> s;

    int health;
    cin >> health;

    if (s) cout << 0 << endl;
    else cout << health << endl;
}