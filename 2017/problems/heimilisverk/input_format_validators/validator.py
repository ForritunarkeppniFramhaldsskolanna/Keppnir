#!/usr/bin/env python

import re
import sys


n = int(sys.stdin.readline())
assert n == len([i for i in sys.stdin])
#assert (len(s) <= 1000 and re.match("^[a-zA-Z]+$", s)) or len(s) == 0
sys.exit(42)
