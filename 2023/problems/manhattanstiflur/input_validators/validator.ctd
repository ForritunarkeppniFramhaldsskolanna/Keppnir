INT(1, 500000, q)
NEWLINE
INT(1, 1000000000, n)
SPACE
INT(1, 1000000000, m)
NEWLINE
REP(q, NEWLINE)
    INT(0, n, x1)
    SPACE
    INT(0, m, y1)
    SPACE
    INT(0, n, x2)
    SPACE
    INT(0, m, y2)
    IF(x1 == x2) 
        ASSERT((y1 - y2 == 1) || (y2 - y1 == 1))
    ELSE
        ASSERT(y1 == y2)
        ASSERT((x1 - x2 == 1) || (x2 - x1 == 1))
    END
END
NEWLINE
EOF
